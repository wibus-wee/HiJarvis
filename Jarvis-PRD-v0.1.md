# Jarvis 产品设计文档（V0.1）

## 1. 文档目的

这份文档用于定义 Jarvis 的产品方向、核心体验、适用场景、边界与阶段目标。

这不是一份技术架构设计文档，也不是一份数据模型文档。本文重点回答的问题是：

* Jarvis 到底是什么产品
* 它应该以什么形态出现
* 用户为什么会需要它
* 它和现有 Agent/多 Agent 产品的区别是什么
* 第一阶段应该做什么，不应该做什么

---

## 2. 产品背景

当前绝大多数 AI 助理产品存在一个共同问题：

它们要么是单一对话窗口中的“大模型助手”，只能在一个上下文里看起来聪明；要么是多 Agent 系统，但一旦扩展到真实使用场景，就会迅速出现组织复杂度上升、记忆污染、角色失控、成本过高、维护困难等问题。

用户真正想要的并不是一个“会聊天的模型”，也不是一个“复杂的 Agent 编排框架”。

用户真正想要的是一种更自然的存在：

* 有时候它像一个单独的私人助理，始终陪在身边
* 有时候它可以分化成多个不同的角色，同时出现在不同场景
* 有时候它可以参与群体讨论、协助推进任务、在不同平台间流转事务
* 它不应该被固定为某一种组织形态
* 它也不应该要求用户先学习一整套 Agent 组织理论才能用起来

Jarvis 的目标，就是提供这样一种“可被组装成多种形态”的智能体产品基础。

---

## 3. 产品定义

Jarvis 不是一个单一 Agent。

Jarvis 是一个可被用户以多种方式调用、组织与使用的智能体产品。

它可以表现为：

* 一个 Telegram 私聊里的个人助理
* Slack 里的多个具名智能角色
* Linear 里的任务执行者
* Discord 里的社区角色或讨论参与者
* 未来也可以扩展到网页、语音、桌面、终端等场景

Jarvis 的核心不是“一个统一大脑”，也不是“一个固定协作协议”。

Jarvis 的核心是：

* 多形态存在
* 多平台出现
* 局部记忆
* 可自由组装
* 不强依赖复杂协作机制
* 对用户来说足够自然，像人，而不是像一个框架

---

## 4. 核心产品主张

### 4.1 单一形态不是目标，多形态共存才是目标

Jarvis 不应该只支持“一个人对一个 Agent 说话”的模式。

它应该天然支持以下情况：

* 用户今天只想和一个 Jarvis 单聊
* 用户明天想在 Slack 里同时和多个 Jarvis 交流
* 用户后天想让某个 Jarvis 去 Linear 推动任务
* 用户也可能希望它出现在 Discord 中参与讨论、社区互动或娱乐

### 4.2 不预设唯一正确组织形态

Jarvis 不应该强迫用户采用某一种 Agent 组织方式。

它不应该默认：

* 只能是单助理模式
* 必须有一个总调度器
* 必须有固定的多 Agent 协议
* 必须采用圆桌、蜂群、Supervisor 之类的显式协作结构

这些都可以存在，但不应该成为产品前提。

### 4.3 记忆不是越统一越好

Jarvis 不追求一个“全局共享大脑”。

不同平台、不同场景、不同身份下的交互，可能本来就彼此无关。强行共享记忆会导致：

* 语境污染
* 角色混乱
* 用户不适
* 工作与生活边界被破坏

因此，Jarvis 的产品哲学是：

**默认局部记忆，必要时才提升。**

### 4.4 重点是组装，不是编排

Jarvis 的产品核心不是设计一个复杂的 Agent 协作系统，而是让不同形态的 Agent 可以自然地被组装出来。

用户使用它时，不应该感受到“这是一个复杂的多 Agent 系统”；用户应该感受到“这些角色就是自然地存在着”。

---

## 5. 目标用户

### 5.1 核心用户

第一阶段的核心用户是对 AI 使用深度较高、愿意尝试新交互形态的人，包括但不限于：

* 高频使用 ChatGPT/Claude/Cursor 等工具的个人用户
* 有大量跨平台沟通与任务协同需求的人
* 想把 AI 从“单次提问工具”变成“持续存在角色”的用户
* 喜欢在不同平台上以不同方式与智能体互动的用户

### 5.2 典型用户特征

这类用户通常：

* 不满足于一个单聊天窗口
* 希望 AI 角色可以持续存在
* 同时有个人生活、工作、社区等多个语境
* 对 Agent 很感兴趣，但不愿意学习一整套复杂系统才能开始使用
* 对“像人”这件事高度敏感

---

## 6. 用户需求

### 6.1 显性需求

* 我想在 Telegram 私聊里拥有一个长期陪伴式助理
* 我想在 Slack 里同时拥有多个具名角色
* 我想让这些角色可以去 Linear 里推动任务
* 我想在 Discord 里也能拥有不同氛围的角色
* 我不想被强迫采用固定组织方式
* 我不想让不同平台的聊天内容互相污染

### 6.2 隐性需求

* 我希望这些存在是活的，而不是工具菜单
* 我希望我能自由决定和一个还是多个角色交互
* 我希望 AI 有连续性，但不是全知全记
* 我希望系统能支持复杂形态，但默认使用方式依然简单
* 我希望产品自然生长，而不是预先设计过重

---

## 7. 产品原则

### 原则 1：单平台体验必须成立

即使用户只在 Telegram 单聊中使用 Jarvis，它也必须是一个完整成立的产品，而不是多 Agent 系统的降级版。

### 原则 2：多实例是自然扩展，不是另一个产品

Slack 中的 many Jarvis、Discord 中的多个角色，不应该是另一套系统，而应当是同一个产品能力的自然展开。

### 原则 3：默认隔离，不默认共享

不同场景下的角色记忆、语境与行为应默认隔离。共享应是受控的、少量的、必要的。

### 原则 4：少做组织设计，多做组装能力

不要过早发明复杂的协作协议。优先保证：

* 能生成不同实例
* 能在不同平台出现
* 能保持局部语境
* 能有不同角色感

### 原则 5：产品体验优先于 Agent 理论正确性

用户不会因为系统“理论上是一个优雅的多 Agent 架构”而喜欢它。用户会因为它自然、稳定、有趣、顺手而喜欢它。

---

## 8. 产品形态

### 8.1 Telegram 单聊：个人助理模式

这是最核心、最基础、也最容易成立的形态。

用户在这里感受到的是：

* 长期陪伴
* 私密关系
* 单一角色沟通
* 事务托管
* 稳定的人格感

这应当是 Jarvis 最先成立、也最重要的产品入口。

### 8.2 Slack：工作模式 / 多角色模式

在 Slack 中，Jarvis 可以以多个角色出现。

重点不在于“多 Agent 编排”，而在于：

* 用户可以自然地与不同角色互动
* 某些角色更偏任务执行
* 某些角色更偏讨论或陪伴式工作支持
* 用户可以只与一个角色沟通，也可以同时与多个角色沟通

Slack 中的存在应该更偏工作场景、团队协同、任务推进，但不要求必须具备严格组织规范。

### 8.3 Linear：任务推进模式

在 Linear 中，Jarvis 更像事务处理者。

它在这里的价值不是“聊天”，而是：

* 建立任务
* 跟进状态
* 整理上下文
* 推动执行
* 衔接其他平台中的讨论结果

Linear 是 Jarvis 的结构化任务表面，而不是人格最强的表面。

### 8.4 Discord：社区/讨论/娱乐模式

Discord 中的 Jarvis 可以更轻松、更社交、更具角色感。

它可以：

* 参与讨论
* 维持某种氛围
* 提供陪伴感
* 在社区中担当某种固定角色

Discord 与 Slack 的区别不只是平台不同，而是氛围不同，因此产品表现也应不同。

---

## 9. 关键产品问题

### 9.1 一个还是多个？

答案不是二选一。

Jarvis 既应该支持“只和一个交流”，也应该支持“很多个同时存在”。

产品不应在这个问题上人为设限。

### 9.2 记忆要不要共享？

默认不共享。

用户在不同平台使用 AI 的目的和语境本来就可能完全不同。对用户来说，“被记住”是好的，但“被错记、被串台”是非常糟糕的体验。

因此：

* Telegram 的个人记忆应当偏局部
* Slack 的工作记忆应当偏局部
* Discord 的社区记忆应当偏局部
* Linear 的任务记忆也应当偏任务性

只有极少数用户明确需要、且确实可迁移的信息，才应该被提升到更高层级。

### 9.3 是不是需要刻意设计 Agent 协作？

不需要。

产品第一阶段不应该花太多精力在显式的 Agent 协作协议上。

用户真正需要的是：

* 角色能出现
* 场景能成立
* 记忆不乱
* 使用自然

只要这些成立，多实例之间自然会形成一些协作或分工。那些更复杂的机制，应该在真实使用出现瓶颈之后再考虑，而不是预先设计进去。

---

## 10. 差异化定位

### 相对于单聊天窗口式 AI 助理

Jarvis 不只是一个对话窗口里的智能体，而是可以跨多个表面存在的持续角色系统。

### 相对于典型多 Agent 框架

Jarvis 不以“编排能力”作为第一卖点，而以“多形态自然存在”作为第一卖点。

### 相对于像 Slock 这样的多角色协作产品

Jarvis 不追求无限生长的组织结构，不要求用户学习复杂的组织方法论。它更强调自然组装、局部边界、按需出现。

### 相对于单一私人助理产品

Jarvis 不被限制在“一个贴身私人助理”的形态中，而是可以扩展为工作角色、社区角色、任务角色等多个存在。

---

## 11. MVP 范围

第一阶段不求“完整”，只求验证核心产品假设。

### MVP 目标

验证以下三件事是否成立：

1. 用户是否真的需要一个可持续存在的单聊 Jarvis
2. 用户是否真的会希望在另一个平台拥有多个具名 Jarvis
3. 局部记忆 + 多表面存在，是否比共享大记忆更符合真实需求

### MVP 建议范围

* 支持 Telegram 单聊
* 支持 Slack 中的多角色出现
* 角色之间默认不共享记忆
* 允许用户自定义角色名称、语气与用途
* 支持最基本的事务能力与对话连续性
* 暂不追求复杂协作机制
* 暂不追求完美跨平台无缝联动

### MVP 不做

* 不做重型 Agent 组织系统
* 不做复杂圆桌/协商协议
* 不做全局统一大脑
* 不做过强自动化执行
* 不做过多平台一次性接入

---

## 12. 用户体验预期

一个理想的用户在使用 Jarvis 时，应该逐渐形成如下感受：

* “我可以拥有一个长期陪着我的角色”
* “我也可以在工作场景中拥有多个不同角色”
* “它们不会莫名其妙串台”
* “我不用先学一套复杂理论才能组织这些角色”
* “这个系统不是在展示架构，而是在自然地陪我、帮我、参与我的生活与工作”

---

## 13. 主要风险

### 风险 1：产品变成复杂系统展示，而不是产品

如果过度强调多 Agent 组织能力，用户会感受到的是复杂，而不是自然。

### 风险 2：平台越多，角色越散

如果没有清晰的产品原则，多平台扩展会导致人格感稀释、体验破碎。

### 风险 3：记忆策略失控

如果默认共享记忆，用户会很快感受到语境污染与边界消失。

### 风险 4：过早追求“什么都支持”

如果一开始就试图把 TG、Slack、Linear、Discord 全做满，容易导致每个场景都不够成立。

---

## 14. 阶段路线

### Phase 1：证明单聊 Jarvis 成立

目标：让 Telegram 单聊中的 Jarvis 成为一个真正可持续使用的个人助理。

### Phase 2：证明多角色工作形态成立

目标：在 Slack 中验证多个具名 Jarvis 是否真的带来更强的工作体验，而不是制造噪音。

### Phase 3：证明任务表面可衔接

目标：让 Jarvis 能以合理方式进入 Linear，承担任务推进与结构化事务角色。

### Phase 4：探索社区角色与氛围角色

目标：在 Discord 中验证角色感、陪伴感与社区存在是否成立。

---

## 15. 最终产品愿景

Jarvis 的最终形态，不是一个单独的聊天机器人，也不是一个复杂的 Agent 编排平台。

它更像一个可以在你的数字生活中长出多种存在形态的智能体产品基底：

* 它可以陪你单独说话
* 可以在工作中以多个角色出现
* 可以在任务系统中推动事情前进
* 可以在社区中形成氛围和陪伴
* 可以根据场景自然变换，而不是被设计死

它的价值，不在于证明某种 Agent 理论，而在于让用户第一次真正拥有“会在不同场合出现的智能体存在”。

---

## 16. 一句话总结

**Jarvis 是一个支持多实例、多表面、多局部记忆、可自由组装的智能体产品，而不是一个被固定为某一种组织形态的 Agent 系统。**
